# v6.3 EverMemOS

This memory system uses a persistent EverMemOS worker subprocess.
Write path: real EverMemOS memorize pipeline.
Read path: real EverMemOS fetch pipeline, with adapter-side query ranking over fetched memory objects.
